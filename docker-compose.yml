
services:
  next-app:
    build:
      context: .
      dockerfile: dockerfile
      args:
        NEXT_PUBLIC_URL_HOST: ${NEXT_PUBLIC_URL_HOST}

    container_name: next-app-phunuttakit

    ports:
      - "3001:3000"

    restart: always

    env_file:
      - .env